import { FC } from 'react';

export const EffectSeparator: FC<{}> = ({ }) => {
    return <div style={{
        position: 'relative',
        top: '-16px',
        background: '#ffffff1a',
        height: '2px'
    }} />;
};